.auth-modal
  .modal-header
    h3.modal-title
      | Se connecter
    p.pull-righ
      = link_to "S'inscrire", new_registration_path(resource_name)
      '
      ||
      '
      = link_to "Mot de passe oublié?", new_password_path(resource_name)
    .clearfix
  .modal-body
    form.form-horizontal[name="authForm"]
      input-email[label="Email" model="auth.email"]
      input-password[label="Mot de passe" model="auth.password"]
      input-boolean[label="Se souvenir de moi" model="auth.remember_me"]
  .modal-footer
    button.btn.btn-primary[ng-click="ok()"]
      | Se connecter
    button.btn.btn-default[ng-click="cancel()"]
      | Cancel
